import React from 'react';
import { Box } from '@chakra-ui/react';
import PhotoCarousel from './PhotoCarousel';

const Home = () => {
  return (
    <Box>
      <PhotoCarousel />
    </Box>
  );
};

export default Home;
